---
- name: Prepare
  hosts: all
  tasks:
    - name: Install docker python module
      community.general.pipx:
        name: "{{ item }}"
      with_items:
        - docker
        - urllib3<2
        - molecule-plugins[docker]
